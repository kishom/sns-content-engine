// テロップ設計（正本 = content/2026-07-22_cat-ckd/02-script.md のシーン割り表）
//
// 台本の「時間」列がカットの尺、「テロップ」列がメインテロップ、「セリフ」列が下部字幕。
// 🐱🐶 は話者判定にのみ使い、画面には出さない（名前チップで話者を示す）。
// **強調** は <em> に変換され、PAWSオレンジ #E8651A で描画される。
//
// t は「そのカットの中での秒数」。end を省略するとカット終端まで。
//
// ── 音声（tts.mjs）について ──
// subs[].text がそのまま読み上げ台本になる（<br>・全角空白は除去される）。
// pyopenjtalk が誤読する語だけ `read:` に**漢字を残したまま**直した文字列を書く
// （全部かなにすると逆にアクセントが崩れる）。誤読の確認方法は README を参照。
// `parts:` を書くと1つの字幕を複数の音声セグメントに分けて合成する（cut-05 のチェックチップ同期用）。
//
// ⚠️ start / end / dur は **無音版の時間割**。音声を付けるときは tts.mjs が
//    実測の音声長からタイムラインを引き直す（voice.timing.json）ので、ここは触らなくてよい。

export const PROJECT = '2026-07-22_cat-ckd';
export const OUT_NAME = 'cat-ckd_reel';

// 画面内免責（省略不可）。病名・症状を出すカットに繰り返し表示する。
// 完視聴しない視聴者にも届かせるため、末尾カットだけに置かないこと。
export const DISCLAIMER = '※一般的な情報です。診断はかかりつけの獣医師へ';

export const CUTS = [
  {
    id: 'cut-01',
    dur: 3.0, // 台本 0-3s（素材4s → トリム）
    // フックは食い気味に返す。ネコが「褒めてる場合じゃないかも」を **2.5秒以内に言い終わる**
    // のがこの台本のリテンション要件（02-script.md）なので、ここだけ間を詰める。
    // ⚠️ この数字を緩めると要件を割る。tts.mjs が毎回検算してログに出す。
    // ⚠️ イヌの話速を 1.40→1.26 に落とした際、ここが 2.52s で要件を割った。
    //    フックだけは「食い気味」が仕様なので、この係数で吸収する（speed 1.10→1.18）。
    tempo: { lead: 0.03, gap: 0.05, speed: 1.18 },
    telop: '猫が水をよく飲む＝<br><em>いいこと？</em>',
    subs: [
      { who: 'dog', text: 'めっちゃ水飲むね！えらい！', start: 0.15, end: 1.45 },
      { who: 'cat', text: '…褒めてる場合じゃないかも', read: '褒めてる場合じゃないかも', start: 1.55 },
    ],
  },
  {
    id: 'cut-02',
    dur: 4.0, // 台本 3-7s（素材5s → トリム）
    // 音声版はセリフ1本しか無く 0.95s の無音が余っていた（＝間延びして見える）。
    // 無音版の 4.0s はそのままに、音声版だけ下限を詰める。
    voiceDur: 3.4,
    // 「あまり」を落として断定形にしない（「飲まないのが普通」と読まれると
    //  飲水量が減った猫の異常を見逃す方向に働く）。字幕の文言と一致させる。
    telop: '猫はもともと<br><em>あまり水を飲まない</em>',
    subs: [
      { who: 'cat', text: '猫はもともと、<br>あまり水を飲まない動物なんだ', start: 0.15 },
    ],
  },
  {
    id: 'cut-03',
    dur: 5.0, // 台本 7-12s（素材6s → トリム）
    telop: '急に水↑ おしっこ↑ は<br><em>体のサイン</em>かも',
    disclaimer: DISCLAIMER,
    subs: [
      { who: 'dog', text: 'えっ、そうなの！？', start: 0.1, end: 1.2 },
      { who: 'cat', text: '「急によく飲む・おしっこが増えた」は体からのサインのことがある',
        read: '「急によく飲む、おしっこが増えた」は体からのサインのことがある', start: 1.3, end: 3.6 },
      { who: 'cat', text: 'これを多飲多尿っていうんだ', start: 3.7 },
    ],
  },
  {
    id: 'cut-04',
    dur: 7.0, // 台本 12-19s（素材6s → ループ延長）
    telop: 'シニア猫に多い<br><em>慢性腎臓病</em>',
    // 病名を出すカット＝最も誤解されやすい。完視聴しない人にも免責が届くようここにも出す。
    disclaimer: DISCLAIMER,
    subs: [
      { who: 'cat', text: '代表的なのが慢性腎臓病。<br>シニア猫にとても多いと言われてるよ', start: 0.15, end: 3.6 },
      { who: 'cat', text: 'ほかの病気のこともあるから、<br>まず変化に気づくことが大事', start: 3.7 },
    ],
  },
  {
    id: 'cut-05',
    dur: 8.0, // 台本 19-27s（素材7s → ループ延長）
    telop: 'この3つを<em>メモ</em>',
    // 3Dシーンのアイコン（水皿 / トイレ / 体重計）の x 中心に合わせて配置。
    // アイコンは上下にゆれるので、ゆれの上限（y≈608）より上のバンドに置く。
    // anchorPart = subs[1].parts[n] の実測開始に合わせる（音声版）。start は無音版のフォールバック。
    checkChips: [
      { text: '水', cx: 208, start: 2.2, anchorPart: 0 },
      { text: 'おしっこ', cx: 587, start: 2.8, anchorPart: 1 },
      { text: '体重', cx: 946, start: 3.4, anchorPart: 2 },
    ],
    subs: [
      { who: 'dog', text: 'どうやって気づけばいいの！？', start: 0.1, end: 1.7 },
      // 1行に収まらないので改行位置を明示（③が単独で行送りされるのを防ぐ）
      {
        who: 'cat', text: '①水を飲む量　②おしっこの量・色<br>③体重', start: 1.8, end: 4.9,
        // 3セグメントに分けて合成する。checkChips の anchorPart が各セグメントの
        // 実測開始時刻に吸着するので、「水」と言った瞬間にチップが出る。
        parts: ['1つめ、水を飲む量', '2つめ、おしっこの量と色', '3つめ、体重'],
      },
      { who: 'cat', text: 'この3つをメモしておくと診察で役立つよ', start: 5.0 },
    ],
  },
  {
    id: 'cut-06',
    dur: 6.0, // 台本 27-33s（素材6s → 等尺）
    telop: '7歳からは<em>定期健診</em>',
    subs: [
      {
        who: 'cat', text: '7歳を過ぎたら、年1〜2回の<br>健康診断（血液・尿検査）で',
        // 「年1〜2回」は トシイチ〜ニカイ と誤読される（〜が読まれない／年がトシになる）。
        // 「1年に1回から2回」で イチネンニイッカイカラニカイ になる。
        read: '7歳を過ぎたら、1年に1回から2回の健康診断、血液や尿の検査で',
        start: 0.15, end: 3.2,
      },
      { who: 'cat', text: '早めに見つけられることがあるよ', start: 3.3 },
    ],
  },
  {
    id: 'cut-07',
    dur: 7.0, // 台本 33-40s（素材6s → ループ延長）
    // 音声版は 1.94s も無音が余っていた。ただしここは保存を促すチェックリストカードなので
    // 「読み切る／スクショする間」を 0.9s ほど残す（0 にすると保存導線が死ぬ）。
    voiceDur: 6.0,
    // チェックリスト再掲カード（テロップの代わり）
    card: {
      heading: 'この3つをチェック',
      rows: ['水を飲む量', 'おしっこの量・色', '体重'],
      pin: 'あとで見返せるように保存',
    },
    disclaimer: DISCLAIMER,
    subs: [
      { who: 'dog', text: 'このチェック、保存しとこ！', start: 0.15, end: 2.4 },
      { who: 'cat', text: '気になる変化があったら、かかりつけの先生に相談してね', start: 2.5 },
    ],
  },
];

// voice = VOICEVOX CORE の話者割り当て。
//   ⚠️ 旧版（pyopenjtalk）は音声が1種類しかないため「同じ声のピッチ違い」で2キャラを
//      作っていた。VOICEVOX では**キャラごとに別の話者**を割り当てられるので、
//      ピッチ／フォルマントの小細工は不要になった。
//
//   styleId         : VOICEVOX のスタイルID（`.venv-voicevox/bin/python scripts/compose/tts.py --speakers` で一覧）
//   label           : 誰の声か（readings.txt とクレジット表記の突き合わせ用）
//   speedScale      : 話速。**話者ごとに素の速さが違うので固定の目安は無い**。
//                     目標モーラ/秒（ショートは 8.5 前後）から逆算して決める。
//   pitchScale      : ピッチ（0=標準。話者が違うので基本 0 でよい）
//   intonationScale : 抑揚（1.0=標準。上げるとキャラが立つが上げすぎると不自然）
//   pauseScale      : 読点「、」の間を何倍にするか（1.0=標準。ショートでは詰める）
//
// ⚠️ 話者を変えたら `content/<project>/voice.credits.md` のクレジット表記も必ず直すこと。
//    VOICEVOX は音声ライブラリごとに利用規約があり、クレジット表記が義務。
export const SPEAKERS = {
  // ネコネコ = のんびり博識なツッコミ役。docs/character.md の「落ち着いた低めトーン・クール」に合わせる。
  cat: {
    name: 'ネコネコ',
    voice: {
      styleId: 9, label: '波音リツ／ノーマル',
      speedScale: 1.07, pitchScale: 0, intonationScale: 1.05, pauseScale: 0.42,
    },
  },
  // イヌイヌ = 元気なボケ役。docs/character.md の「明るく勢いがある」に合わせて高く速い声。
  dog: {
    name: 'イヌイヌ',
    voice: {
      styleId: 8, label: '春日部つむぎ／ノーマル',
      // 1.40（9.2モーラ/秒）は速すぎるとのフィードバック（2026-07-26 Kisho）。
      // ネコ（8.4）より速い＝元気なボケ役、という性格差は残しつつ落ち着かせる。
      speedScale: 1.26, pitchScale: 0, intonationScale: 1.10, pauseScale: 0.40,
    },
  },
};

// ⚠️ 話者選びで踏んだ罠（次に変えるとき必ず読むこと）
//
// 1) **VOICEVOX は話者ごとに素の話速が全く違う。** 同じ speedScale でも
//    九州そら は 9.1 モーラ/秒、波音リツ は 13.6 モーラ/秒だった（同一文で実測）。
//    ∴ speedScale は「キャラの速さ」ではなく **目標モーラ/秒から逆算する係数**。
//    素が速い話者を選ぶほど引き伸ばし量が減り、音質が良くなる（今回 1.63 → 1.07 に低減）。
//
// 2) **キャラの速さの差は speedScale の大小では表せない。** 実際に効くのは
//    出来上がりのモーラ/秒（実測 ネコ 8.4 / イヌ 9.2）。tts.mjs が毎回ログに出すのでそれで見る。
//
// 3) **ピッチは必ず実測で確認する。** 最初 九州そら×白上虎太郎 にしたが F0 が
//    258Hz と 255Hz でわずか 0.2 半音差＝2キャラが聞き分けられなかった。
//    verify_voice.mjs の「2キャラのピッチ差 ≧ 2.0 半音」はこれを捕まえるための検査。
//    character.md の「ネコ＝低め／イヌ＝明るい」を満たすには **ネコ < イヌ** にすること。
